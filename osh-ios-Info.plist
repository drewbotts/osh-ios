<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- Everything else is generated from INFOPLIST_KEY_* build settings.
	     UIBackgroundModes has no INFOPLIST_KEY_ alias, and GPSOutput traps at
	     runtime if allowsBackgroundLocationUpdates is set without it, so this
	     file exists purely to declare it. -->
	<key>UIBackgroundModes</key>
	<array>
		<string>location</string>
	</array>
</dict>
</plist>
